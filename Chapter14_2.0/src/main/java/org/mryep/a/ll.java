package org.mryep.a;

import java.util.LinkedList;

// 修改类名为 LinkedListExample
public class ll {
    @SuppressWarnings({"all"})
    public static void main(String[] args){
        // CRUD
        // 修改变量名，避免与类名冲突
        LinkedList<Integer> list = new LinkedList<>();
        list.add(1);
        list.add(2);
        System.out.println(list);
    }
}
