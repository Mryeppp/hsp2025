package org.mryep.a;

import java.util.Properties;

public class pp {
    @SuppressWarnings({ "all" })
    //properties 是hashtable的子类 常用于properties配置文件的读取 键和值都是字符串类型
    public static void main(String[] args) {
        Properties p = new Properties();
    }
}

